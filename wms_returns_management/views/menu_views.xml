<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Actions -->
    <record id="action_wms_return_authorization_list" model="ir.actions.act_window">
        <field name="name">Return Authorizations</field>
        <field name="res_model">wms.return.authorization</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new return authorization
            </p><p>
                Manage return merchandise authorizations (RMA) for customer returns.
            </p>
        </field>
    </record>

    <!-- Returns Management Menu (under Customer Management > Operations) -->
    <menuitem id="menu_wms_returns_management_root"
              name="Returns Management"
              parent="wms_owner.menu_3pl_customer_operations"
              sequence="20"/>

    <menuitem id="menu_wms_returns_authorizations"
              name="Return Authorizations"
              parent="menu_wms_returns_management_root"
              action="wms_returns_management.action_wms_return_authorization_list"
              sequence="10"/>
</odoo>